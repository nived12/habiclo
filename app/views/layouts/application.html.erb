<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || t("app.name") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="<%= t("app.tagline") %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="preload" href="/fonts/Fraunces.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="preload" href="/fonts/IBMPlexSans-Regular.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="preload" href="/fonts/IBMPlexMono-Regular.woff2" as="font" type="font/woff2" crossorigin>

    <link rel="icon" href="/icon.svg" type="image/svg+xml">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <style>:root { --brand-hue: <%= (current_or_guest_user&.brand_hue || 25) %>; }</style>
  </head>

  <body data-controller="keyboard-nav brand-hue help tz-detect"
        data-help-seen-value="<%= current_or_guest_user&.help_seen?.to_s %>"
        data-help-ack-url-value="<%= help_acknowledgment_path %>">
    <%= render "shared/topbar" %>
    <%= render "shared/guest_banner" if current_or_guest_user&.guest? %>
    <% if flash[:notice].present? %>
      <div class="flash-notice" data-controller="flash" data-action="click->flash#dismiss animationend->flash#dismiss"><%= flash[:notice] %></div>
    <% end %>
    <% if flash[:alert].present? %>
      <div class="flash-alert" data-controller="flash" data-action="click->flash#dismiss"><%= flash[:alert] %></div>
    <% end %>

    <main class="almanac-page">
      <%= yield %>
    </main>

    <%= turbo_frame_tag "log_modal" %>
    <%= turbo_frame_tag "health_modal" %>
    <%= render "shared/help_panel" %>
  </body>
</html>
