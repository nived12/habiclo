#!/bin/bash -e

# Enable jemalloc for reduced memory usage and latency.
if [ -z "${LD_PRELOAD+x}" ]; then
    LD_PRELOAD=$(find /usr/lib -name libjemalloc.so.2 -print -quit)
    export LD_PRELOAD
fi

# If running the rails server then create or migrate existing database.
# IMPORTANT (deploy gotcha): this auto-migration ONLY fires when the command
# ends in exactly `./bin/rails server` (the Dockerfile's default CMD, served via
# Thruster). Do NOT set a Railway "Custom Start Command" / railway.json
# startCommand — it both fails this condition (so migrations never run) and
# bypasses Thruster, leaving the container up with no Rails process (502).
# New migrations (Ahoy, Solid Queue, etc.) apply automatically here on deploy.
if [ "${@: -2:1}" == "./bin/rails" ] && [ "${@: -1:1}" == "server" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
